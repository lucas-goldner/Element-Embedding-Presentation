import 'package:flutter/material.dart';

class WebViewWeb extends StatelessWidget {
  const WebViewWeb({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('MacOs');
  }
}
